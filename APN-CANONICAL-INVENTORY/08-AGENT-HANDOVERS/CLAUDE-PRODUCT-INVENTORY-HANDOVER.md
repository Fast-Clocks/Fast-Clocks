# CLAUDE — PRODUCT INVENTORY HANDOVER

**Scope:** `TARGETED GOOGLE DRIVE RESEARCH — NOT FULL T7 OR FILESYSTEM INVENTORY.`
**Mode:** read-only. No Drive source file, Supabase object, price, product or deployment was modified.
**Date:** 2026-07-23 · **Reviewer:** Claude (product/Drive lane).

## What this is
The completed Drive product research rendered into the **canonical, machine-readable envelope** shared with Codex, so product records merge directly with Codex's estate inventory and with Claude's backend inventory.

## Files (all under `APN-CANONICAL-INVENTORY/`)
| File | Rows | Purpose |
|---|---|---|
| `PRODUCT-UNIVERSE.json` | 41 | Full envelope records (shared top-level fields; `product_detail` + `backend_detail` children) |
| `PRODUCT-UNIVERSE.yaml` | 41 | Same, YAML |
| `PRODUCT-UNIVERSE.csv` | 41 | Flat merge view |
| `PRODUCT-BACKEND-CROSSWALK.csv` | 41 | product ↔ SUP- backend IDs + integration state |
| `PRODUCT-CONTRADICTIONS.csv` | 14 | Contradictions recorded, **not** resolved |
| `PRODUCT-SOURCE-EVIDENCE.csv` | 100+ | product ↔ Drive doc name + drive_id + evidence label |

## ID scheme
Stable IDs `APN-PROD-0001 … APN-PROD-0041`, assigned in lineage order (ADR → DWD → APN → Sovereign → Filing Machine → Verticals). Aliases and lineage preserved in `product_detail.aliases` and `lineage`.

## Status vocabulary used (per record `current_status`)
`DEPLOYED` (7) · `PARTIALLY DEPLOYED` (9) · `DEMONSTRATION` (1) · `PLANNED` (18) · `UNVERIFIED` (4) · `RETIRED` (1) · `DUPLICATE` (tracked via `duplicate_group_id`, not as a status). Maturity is skewed to design/concept — only a handful are truly live.

## Envelope conformance
- Shared top-level field names are **unchanged** from the common envelope.
- Product-specific data (era, aliases, price_variants, per-source evidence) lives in **`product_detail`**.
- Backend linkage (SUP- IDs, integration_state) lives in **`backend_detail`** — same slot Claude's backend inventory uses.
- `source_project`/`source_repository`/`source_folder` = `AWAITING_CODEX` (this is Drive research, not a repo scan).

## Backend crosswalk highlights
- `APN-PROD-0027` APN Backbone → **SUP-PROJ-SYDNEY / SUP-TABLE-LEDGER-EVENTS** — VERIFIED CONNECTED.
- `APN-PROD-0011` Free Resource Hub → **SUP-TABLE-RESOURCES** — VERIFIED CONNECTED.
- `APN-PROD-0019` Tech Startup Hub / Jobs → jobs/ads/inquiries/packages tables — CONNECTED BUT INCOMPLETE (tables empty).
- `APN-PROD-0028` APN Ledger Trust → **SUP-RECON-LEDGER-LOCATION** — WRONG BACKEND / UNKNOWN (the doc's `ledger_entries`/Merkle/Ed25519 model is **not** in the visible Supabase account).
- `APN-PROD-0034` SafePet → Lovable backend, **not** Supabase Sydney — WRONG BACKEND / UNKNOWN.
- `APN-PROD-0006` darkwebscanner → its own **compromised/quarantined** repo backend — DISCONNECTED.

## Contradictions (recorded, not resolved — per instruction)
Pricing (≥5 ladders, CON-001), Founding price (CON-002), DIY price (CON-003), audit price (CON-004), entity ABN↔ACN (CON-005), incorporation date (CON-006), resource count (CON-007), DataTrace purpose (CON-008), File Brain naming (CON-009), payments live↔test (CON-010), tamper-evidence claim (CON-011, matches backend FIND-004), Suite/Hub/Forge duplication (CON-012), Hugo maturity (CON-013), SafePet verified↔pending (CON-014).

## Honesty notes
- Historical price / entity / "live" claims are evidence of what was written (Mar–Jul 2026), **not** confirmed current truth.
- One evidence doc — the **June Commercial Pricing Model** (owner `kiwichefchris@gmail.com`) — was referenced by the research but its `drive_id` was not captured; marked `drive_id pending — Codex`.
- Several Sovereign estate items (`APN-PROD-0032`) are name-only triage lines → `UNVERIFIED`, needing Codex/filesystem confirmation.

_Generated by [Claude Code](https://claude.ai/code)_
