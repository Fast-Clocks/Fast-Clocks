# CODEX ↔ CLAUDE BACKEND RECONCILIATION

Claude owns the **backend** lane; Codex owns the **filesystem / repository / product / commercial** lane. This file lists what Claude assigned temporary `SUP-` IDs to, what Claude needs from Codex, and the merge procedure. **Do not produce a second competing canonical inventory** — these records merge into Codex's canonical set by the rules below.

## Temporary backend IDs (AWAITING_CODEX_RECONCILIATION)
| SUP- id | object | needs Codex to supply |
|---|---|---|
| SUP-PROJ-SYDNEY | apn-backbone-sydney (`plhpjktkzsbfnltemhdl`) | product(s)/repos/Lovable projects/domains that point here |
| SUP-PROJ-FASTCLOCKS | Fast-Clocks's Project (`tyorcdwpwoxaqwsbgybm`, INACTIVE) | what this project was; any repo referencing it |
| SUP-FN-DOMAIN-WATCH / SUP-FN-LINK-AUDIT | Edge Functions | repo path + source hash for drift compare |
| SUP-TABLE-* (21 tables) | schema objects | which product reads/writes each; repo migration source |
| SUP-RECON-LEDGER-LOCATION | the doc's `ledger_entries`/`verification_records` schema | **which Supabase project backs "APN Ledger Trust" (Lovable 54595d8a)** — not in org `upzwwfqvxuijrldohzwv` |
| SUP-RECON-MIGRATIONS | 7 UUID-named migrations (2026-07-17→21) | the actual migration SQL from the repo(s) |

## Exact questions Codex can answer from the filesystem
1. Which repository(ies) contain Supabase config (`supabase/config.toml`, `supabase/migrations/`) pointing at `plhpjktkzsbfnltemhdl`?
2. Do multiple Lovable projects keep **conflicting** migration folders for this same DB?
3. Do generated TypeScript types in any repo match the live schema (21 tables, enums `inbox_queue_status`)? Any repo with **stale** types pointing at this live DB?
4. Where is the "APN Ledger Trust" Supabase (the Ed25519/Merkle ledger)? Separate account/org?
5. Which products are meant to be **backend-free** (new APN front door, Filing Cabinet v0.1)?

## Files Claude needs from Codex to finish reconciliation
`INVENTORY.json`, `PRODUCT-UNIVERSE.csv`, `APN-NETWORK-BLUEPRINT.md`, `LOVABLE-HANDOVER.md`, `SOURCE-PRIORITY.md`, and each repo's `supabase/migrations/` listing.

## Merge procedure (when Codex canonical inventory is available)
1. match by repository path → 2. match by product name → 3. match by project ref → 4. match by migration hash → 5. match by source-file hash → 6. replace `SUP-` IDs with canonical IDs → 7. retain `SUP-` IDs as aliases → 8. record reconciliation date + confidence.

_Generated by [Claude Code](https://claude.ai/code)_
