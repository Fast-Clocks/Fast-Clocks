# LOVABLE BACKEND BOUNDARIES

Rules for Lovable while Claude reconciles the backend. Evidence: 7 UUID-named migrations on the live Sydney DB indicate Lovable **has** authored schema changes directly (dual ownership with named CLI/Claude migrations).

## Connection map (LIVE-DB + to be confirmed by Codex)
- **apn-backbone-sydney** (`plhpjktkzsbfnltemhdl`) is the single canonical backend for the resource hub, domain register, link-audit, ledger spine and inbox v0.
- Products that currently use it: APN Public Resource Hub, Sovereign Showcase / domain sales, Inbox Guardian (schema only). Exact product↔repo mapping: **AWAITING_CODEX**.

## Must remain backend-free (initially)
- **New APN public front door** — static, no Supabase.
- **APN Filing Cabinet v0.1** — local-first, no Supabase.

## Permission bands per product
- May **read public data** (anon SELECT, RLS-gated): resource hub, domain listings, packages, verticals, brands, jobs, ads.
- May **submit** (anon INSERT, validated): resource submissions, inquiries, waitlist.
- Requires **authenticated** access: inbox (owner-isolated by `auth.uid`).
- Requires **service functions** (never anon): ledger reads/writes, accept-submission, erasure, retention, audit pipeline.

## Prohibitions while reconciliation is in progress
- Do **not** apply migrations independently (freeze Lovable-authored migrations on this DB).
- Do **not** create a second unapproved Supabase project.
- Do **not** change RLS, grants, or the ledger triggers.
- Do **not** move a product to a different backend.
- Public pages **link to** products; they must not duplicate sensitive backend logic.
- Migration ownership + RLS ownership sit with the backend lane (Claude proposes; Chris approves) until reconciled.
- Environment variables: names only in any committed file (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`) — never values.
- Prohibited claims on any Lovable surface: "immutable", "blockchain", "trusted timestamp", "legally binding", "publicly verifiable" (the last is not yet true — verifier fns are not anon-exposed).

## Testing requirements before any new integration
Individually approved; must show: RLS proof (anon cannot read what it shouldn't), no service key in client bundle, and no new anon-executable SECURITY DEFINER function without a privacy review.

_Generated by [Claude Code](https://claude.ai/code)_
