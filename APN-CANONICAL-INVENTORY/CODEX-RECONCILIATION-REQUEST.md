# CODEX RECONCILIATION REQUEST — from Claude's product + backend lanes

**Scope reminder:** Claude's inventory is `TARGETED GOOGLE DRIVE RESEARCH + LIVE SUPABASE (read-only)` — **NOT** a full T7 / filesystem / repo scan. The items below need Codex's filesystem/repo authority to verify. Nothing here authorises changes.

## 1. From `/Volumes/T7` (archive volume — treat read-only)
- Locate original **Assay Seal executables**: `seal.html`, `verify.html`, `test.mjs` (missing from Drive; `APN-PROD-0025` cannot be reproduced without them).
- Locate the **June Commercial Pricing Model** source (owner kiwichefchris@gmail.com) and return its Drive/file ID (`CON-001`).
- Any **`ledger_entries` / `verification_records` / Merkle / Ed25519 `sign-manifest`** source (the Sovereign Engine doc's ledger) — confirm it is NOT the Sydney `apn_ledger_events` (`APN-PROD-0028`, `SUP-RECON-LEDGER-LOCATION`).

## 2. From the local filesystem
- Confirm which products are meant to be **backend-free** (new APN front door, Filing Cabinet v0.1).
- Resolve the 5 duplicate **`filing-machine`** docs → one canonical (`APN-PROD-0033`, `CON-009`); confirm whether "File Brain" == "Watchkeeper's Filing Machine".
- Verify the name-only triage items in `APN-PROD-0032` (AssistHub AI, Launch Ready Review, APN Surgery Suite, Project Hub Navigator, Account Consolidation Hub, The Inner Critic, **Friendly Hub Finder** [flagged published — unpublish], Your Project Story, APN Lighthouse Vision).

## 3. From Git repositories
- Map each product to its **repo path + remote**; fill `source_repository` / `source_folder` (currently `AWAITING_CODEX`).
- Identify the **compromised `robinson-tech-platform`** repo (wallet-stealer in `postcss.config.mjs`) backing `APN-PROD-0006` darkwebscanner; confirm quarantine.
- Confirm **`Fast-Clocks`** = clean production repo for APN.
- Diff the **7 UUID-named Supabase migrations** (2026-07-17→21) against repo `supabase/migrations/` — their SQL is unverified (backend `SUP-RECON-MIGRATIONS`).

## 4. From Lovable projects
- `perthsafepet.lovable.app` (`APN-PROD-0034`) — which Lovable project + which Supabase project backs it (NOT Sydney)? Confirm sandbox Stripe.
- Sovereign Suite / Hub / Forge (`APN-PROD-0031`) — which Lovable projects; identify the Hub→Suite merge and the Forge duplicate to retire (`CON-012`).
- The APN Ledger Trust Lovable project (`54595d8a-…`) — its Supabase project ref (the ledger backend not visible in org `upzwwfqvxuijrldohzwv`).

## 5. From deployment configuration
- For each `current_url` / Vercel project, confirm live vs preview vs dormant; reconcile with the 4 Vercel projects seen on this repo (`fast-clocks` live; `apn-hub-restored`, `apn-privacy-atlas`, `public-access-hub` erroring).
- Confirm which Stripe accounts are **test vs live** (`CON-010`) across ADR, DWD, SafePet.

## 6. Missing executable / source bundles to recover
- Assay Seal `seal.html` / `verify.html` / `test.mjs`.
- Sovereign `ledger_entries` schema + `sign-manifest` function source.
- Filing Machine bundle's real assets (current links are placeholder `docs.google` paths).

## Merge keys
Match Claude's `APN-PROD-####` product IDs and `SUP-*` backend IDs to Codex canonical IDs by: repo path → product name → project ref → migration hash → source-file hash. Retain Claude IDs as aliases; record reconciliation date + confidence. **Do not resolve pricing or entity contradictions** — carry them as `CON-###` until Chris decides.

_Generated by [Claude Code](https://claude.ai/code)_
